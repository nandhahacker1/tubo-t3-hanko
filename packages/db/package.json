{
  "name": "@repo/db",
  "version": "0.1.0",
  "main": "./index.ts",
  "types": "./index.ts",
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf .turbo node_modules",
    "with-env": "dotenv -e ../../.env --",
    "dev": "bun with-env prisma studio",
    "db-push": "bun with-env prisma db push",
    "db-generate": "bun with-env prisma generate",
    "build":"bun with-env prisma generate && bun with-env prisma db push"
  },
  "dependencies": {
    "@prisma/client": "^4.6.1"
  },
  "devDependencies": {
    "dotenv-cli": "^6.0.0",
    "prisma": "^4.6.1",
    "typescript": "^4.9.3"
  }
}
